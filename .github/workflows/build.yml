name: Build and Release

on:
  push:
    branches: [ main, master ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main, master ]
  merge_group:
    types: [checks_requested]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      version_name: ${{ steps.version.outputs.version_name }}
      version_code: ${{ steps.version.outputs.version_code }}
      should_deploy: ${{ steps.version.outputs.should_deploy }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v6
      with:
        fetch-depth: 0

    - name: Set up JDK 17
      uses: actions/setup-java@v5
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v5
      with:
        cache-read-only: ${{ github.ref != 'refs/heads/main' }}

    - name: Calculate version
      id: version
      run: |
        # Determine if we should deploy (main branch or tag)
        if [[ "$GITHUB_REF" == "refs/heads/main" ]] || [[ "$GITHUB_REF" == "refs/heads/master" ]] || [[ "$GITHUB_REF" == refs/tags/* ]]; then
          echo "should_deploy=true" >> "$GITHUB_OUTPUT"
        else
          echo "should_deploy=false" >> "$GITHUB_OUTPUT"
        fi

        # Generate version based on context
        if [[ "$GITHUB_REF" == refs/tags/* ]]; then
          # Manual tag - use tag version
          VERSION_NAME=${GITHUB_REF#refs/tags/v}
          VERSION_CODE=$(date +%s)
          echo "Version from tag: $VERSION_NAME"
        else
          # Auto-version based on date and time
          DATE_VERSION=$(date -u +%Y.%m%d.%H%M)
          VERSION_NAME="$DATE_VERSION"
          # Version code: minutes since epoch (fits in 32-bit int)
          VERSION_CODE=$(( $(date +%s) / 60 ))
          echo "Auto-version: $VERSION_NAME (code: $VERSION_CODE)"
        fi

        echo "version_name=$VERSION_NAME" >> "$GITHUB_OUTPUT"
        echo "version_code=$VERSION_CODE" >> "$GITHUB_OUTPUT"

    - name: Update build.gradle with version
      if: steps.version.outputs.should_deploy == 'true'
      run: |
        VERSION_NAME="${{ steps.version.outputs.version_name }}"
        VERSION_CODE="${{ steps.version.outputs.version_code }}"

        # Update versionCode and versionName in build.gradle
        sed -i "s/versionCode [0-9]*/versionCode $VERSION_CODE/" app/build.gradle
        sed -i "s/versionName \"[^\"]*\"/versionName \"$VERSION_NAME\"/" app/build.gradle

        echo "Updated build.gradle:"
        grep -A 2 "versionCode\|versionName" app/build.gradle

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build Debug APK
      if: steps.version.outputs.should_deploy == 'false'
      run: ./gradlew assembleDebug

    - name: Build Release APK
      if: steps.version.outputs.should_deploy == 'true'
      run: ./gradlew assembleRelease

    - name: Sign Release APK
      if: steps.version.outputs.should_deploy == 'true'
      run: |
        # Create keystore from secret
        echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > release.keystore

        # Sign the APK
        "${ANDROID_HOME}/build-tools/35.0.1/apksigner" sign \
          --ks release.keystore \
          --ks-key-alias ${{ secrets.KEY_ALIAS }} \
          --ks-pass pass:${{ secrets.KEYSTORE_PASSWORD }} \
          --key-pass pass:${{ secrets.KEYSTORE_PASSWORD }} \
          --out app/build/outputs/apk/release/app-release-signed.apk \
          app/build/outputs/apk/release/app-release-unsigned.apk

        # Clean up keystore
        rm release.keystore

    - name: Rename APK with version
      if: steps.version.outputs.should_deploy == 'true'
      run: |
        VERSION_NAME="${{ steps.version.outputs.version_name }}"
        mv app/build/outputs/apk/release/app-release-signed.apk \
           app/build/outputs/apk/release/offtiktok-redirector-${VERSION_NAME}.apk

    - name: Upload Debug APK
      if: steps.version.outputs.should_deploy == 'false'
      uses: actions/upload-artifact@v5
      with:
        name: app-debug
        path: app/build/outputs/apk/debug/app-debug.apk

    - name: Upload Release APK
      if: steps.version.outputs.should_deploy == 'true'
      uses: actions/upload-artifact@v5
      with:
        name: app-release-${{ steps.version.outputs.version_name }}
        path: app/build/outputs/apk/release/offtiktok-redirector-${{ steps.version.outputs.version_name }}.apk

  create-release:
    needs: build
    runs-on: ubuntu-latest
    if: needs.build.outputs.should_deploy == 'true' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && github.event_name != 'merge_group'
    permissions:
      contents: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Download Release APK
      uses: actions/download-artifact@v7
      with:
        pattern: app-release-*
        path: apks
        merge-multiple: true

    - name: Generate changelog
      id: changelog
      run: |
        # Get commits since last release
        LAST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")

        if [ -z "$LAST_TAG" ]; then
          CHANGELOG=$(git log --pretty=format:"- %s (%an)" -10)
        else
          CHANGELOG=$(git log "${LAST_TAG}..HEAD" --pretty=format:"- %s (%an)")
        fi

        # Save to file to handle multiline
        echo "$CHANGELOG" > changelog.txt

    - name: Create or Update Release
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        VERSION_NAME: ${{ needs.build.outputs.version_name }}
      run: |
        CHANGELOG=$(cat changelog.txt)
        APK_FILE="apks/offtiktok-redirector-${VERSION_NAME}.apk"
        RELEASE_TAG="v${VERSION_NAME}"

        # Check if release already exists
        if gh release view "$RELEASE_TAG" &>/dev/null; then
          echo "Release $RELEASE_TAG exists, updating..."
          gh release upload "$RELEASE_TAG" "$APK_FILE" --clobber
        else
          echo "Creating new release $RELEASE_TAG..."
          gh release create "$RELEASE_TAG" \
            "$APK_FILE" \
            --title "OffTikTok Redirector v${VERSION_NAME}" \
            --notes "$(cat <<EOF
        ## OffTikTok Redirector v${VERSION_NAME}

        **Auto-released from main branch**

        A lightweight app that redirects TikTok links to OffTikTok in your browser.

        ### Recent Changes
        ${CHANGELOG}

        ### Installation
        1. Download the APK below
        2. Enable 'Install from Unknown Sources' if needed
        3. Install and enjoy!

        ### Features
        - Zero permissions required
        - No external dependencies
        - Ultra-lightweight
        - Handles all TikTok domains
        - Works with share menu
        EOF
        )"
        fi

        echo "✅ Release published: $RELEASE_TAG"

  create-manual-release:
    needs: build
    runs-on: ubuntu-latest
    if: needs.build.outputs.should_deploy == 'true' && startsWith(github.ref, 'refs/tags/') && github.event_name != 'merge_group'
    permissions:
      contents: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Download Release APK
      uses: actions/download-artifact@v7
      with:
        pattern: app-release-*
        path: apks
        merge-multiple: true

    - name: Create Release for Tag
      uses: softprops/action-gh-release@v2
      with:
        files: |
          apks/offtiktok-redirector-${{ needs.build.outputs.version_name }}.apk
        body: |
          ## OffTikTok Redirector ${{ github.ref_name }}

          Download the APK below.

          ### Changes
          See commit history for details.
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
